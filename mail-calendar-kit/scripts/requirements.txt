# Зависимости пака «Второй мозг ↔ почта и календарь». Ставятся в ~/.venvs/mck скриптом setup_venv.sh.
caldav>=1.3
icalendar>=5.0
# Разворачивает повторяющиеся встречи из .ics (еженедельные планёрки и т.п.)
recurring-ical-events>=2.1
# Корневые сертификаты: без них Python в отдельном окружении не открывает https на Маке
certifi
python-dotenv>=1.0
# Google Calendar (нужны только при CAL_PROVIDER=google, но ставятся сразу — ~40 МБ)
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
# Notion (необязательный шаг)
notion-client>=2.2
# База часовых поясов: на slim-серверах и Windows без неё zoneinfo не работает
tzdata
