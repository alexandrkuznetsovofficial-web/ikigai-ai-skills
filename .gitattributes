# Скрипты китов доезжают до ученика в правильных концах строк:
# probe.sh запускается в Git Bash и с CRLF умрёт на первой строке,
# probe.ps1 читается Windows PowerShell и хочет CRLF.
*.sh text eol=lf
*.ps1 text eol=crlf
*.md text
